# ============ Node.js ============
node_modules/
pnpm-lock.yaml
package-lock.json
yarn.lock

# ============ Build Outputs ============
dist/
build/
*.局部构建

# ============ Rust / Tauri ============
src-tauri/target/
src-tauri/Cargo.lock

# Rust 编译产物（保留 target/ 但忽略其内容）
# 上面已经忽略了整个 target/，如果需要可以改为：
# src-tauri/target/debug/
# src-tauri/target/release/

# .cargo 配置（可选，保留用户本地配置）
# .cargo/

# Tauri 桌面应用构建产物
*.app
*.dmg
*.exe
*.msi
*.deb
*.rpm
*.AppImage

# ============ IDE / Editor ============
.idea/
*.iml
.vscode/
*.swp
*.swo
*~
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# ============ Environment ============
.env
.env.local
.env.*.local

# ============ Logs ============
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============ Testing ============
coverage/

# ============ Tauri Specific ============
src-tauri/gen/schemas/

# ============ Vite ============
.vite/
vite.config.ts.timestamp-*
vite.config.ts.*.timestamp
